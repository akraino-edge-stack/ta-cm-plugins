[access]
	inheritFrom = ta
[access "refs/*"]
	owner = group ldap/akraino-gerrit-ta-cm-plugins-committers
[project]
	description = cm plugins store all the existing AKREC plugins slotted into the configuration manager framework. There are four distinct plugin types
